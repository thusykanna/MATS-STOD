# Parent-cap ablation: keep at most 16 parents per segment.
# The cap is applied after inference, so a sweep re-uses the cache and costs
# nothing. See D30 for why the rule keeps the nearest parents.
run_name: cap_16
graph:
  max_parents: 16
